# SDD progress

Task 1: complete (commits 8a7ff73..fd326d8, review clean). Minor: dual conftest path setup; timeline_view.py main() not hooked (out of scope).
Task 2: complete (commits fd326d8..10c4165, review clean). Minor: unused Path/_which_missing in tests.
Task 3: complete (commits 10c4165..65c9254, review clean). Minor: mid-file import in render.py; unused validate_edl in pipeline (later tasks).
Task 4: complete (commits 65c9254..2043bca, review clean). Minor: non-atomic save_session; reclaim mutates in place.
Task 5: complete (commits 2043bca..7a166ce, review clean). Minor: run_helper untested; transcribe_batch still case-exact.
Task 6: complete (commits 7a166ce..d8b6502, review clean). Minor: no rendering/mtime-stale tests.
Task 7: complete (commits d8b6502..b286695, review clean). Minor: CORS only localhost:5173; state polls live doctor+ffprobe.
Task 8: complete (commits b286695..c399bc1, review clean). Minor: pid==1 test sentinel in production; live doctor on transcribe route.
Task 9: complete (commits c399bc1..4a67f4d, review clean). Minor: sync SSE can stall API; reject mid-stream can lose note; SKILL.md not also in The process.
Task 10: complete (commits 4a67f4d..7ad05b4, review clean after fix). Minor: leftover staging file; os.replace if player locks preview.mp4.
Task 11: complete (commits 7ad05b4..a811e0d, review clean). Minor: preview URL cache; action buttons re-enable after POST; Vite leftovers.
Task 12: complete (commits a811e0d..a148d3a, review clean after fix). Minor: no SSE unit test; duplicated reader.
Task 13: complete (commits a148d3a..4c9fe4c, review clean). Minor: uvicorn may outlive the shell.
Task 14: complete (commits 4c9fe4c..3b48794, smoke partial). Doctor+pytest+vitest+UI empty state green. ExecutionPolicy Bypass documented. Remaining: user footage → Transcribe → chat → Approve → revision → final.
Final-review fixes: 50628f7 (retry error event, preview cache-bust, acceptEdits)
All tasks complete. Tests: 45 pytest + 4 vitest passed.
Task 9: complete (commits c399bc1..4a67f4d). Minor: live doctor on chat enable; route/stream untested in-repo; pid=1 busy guard.
Final-review fixes: 50628f7 (retry error event, preview cache-bust, acceptEdits)



---

# Plan: 2026-08-17-clipcut-foundation (branch feat/clipcut-foundation)

Merge base: 59404c3. Ledger entries above this line belong to the completed
2026-08-16-video-use-local-app plan and are NOT progress on this plan.

Task 1: complete (commits 59404c3..da80327, review clean after fix). Minor: claim atomicity test is sequential not concurrent; no jobs indexes yet; no-op on unknown job_id. NOTE: subagents used 'git add -A' and committed .venv-local/ (2891 files) — recommitted clean, .gitignore updated. All later dispatches must stage explicit paths.
Task 2: complete (commits da80327..20c2f49, review clean after fix). Added jobs.cancel() helper. Lost-lease = warn only, deliberate (single-worker). Minor: distinct() scan per poll; unregistered kinds claimed+failed (safe: one unified worker binary).
Task 3: complete (commits 20c2f49..f551c67, review clean, no fix needed). Also dropped dead 'import transcription' from server.py (verified safe). Minor: handler failure test does not assert job status; _register_handlers spacing.
Task 4: complete (commits f551c67..2144845, review clean after fix). Threads fully gone from server.py. Created backend/cut_state.py to break worker->API dependency (worker no longer pulls FastAPI+2nd MongoClient). Cancel now resets project export doc. Minor deferred to final review: Cloudinary branch untested.
Task 4b: CRITICAL fix (commit 341deed). Running "python worker.py" made module __main__, so handlers doing "import worker" registered into a SECOND module copy — worker started with handlers: [] and would have claimed+failed every job. Cancelled class identity was also split. Fixed by re-entering via "from worker import main". Regression test test_running_as_script_registers_all_handlers spawns the script as a subprocess. Unit tests could not catch this (they import worker once).
E2E VERIFIED after Task 4: upload -> transcribe (71 words) -> export via queue. Output 1080x1920, 15.07s, 6588491 bytes — byte-identical to pre-refactor. Cloudinary ok. Job status/progress tracked via GET /api/jobs/{jid}.
Task 5: complete (commits 341deed..02961e9, review clean after fix). Version-mismatch branch now tested. Minor: no docstrings on new_plan/overlay; missing reframe/captions keys silently default.
Task 6: complete (commits 02961e9..71b7cad, review clean after fix). clean() now has real containment guard (rejects '..' parts, asserts resolved edit path is direct child named 'edit') + escape test with sentinel. Minor: 'import pytest' inside test body.
BASELINE (verified at merge base 59404c3 in a temp worktree): root tests/ = 77 passed, 6 failed. The 6 are pre-existing from the prior plan and NOT caused by this branch: test_api.py x2 (400 vs 409), test_claude.py test_first_turn_has_no_resume, test_cut_picks.py test_apply_cut_picks_zooms_and_drops, test_talking_head.py test_apply_bin_inserts_broll, test_visual_picks.py test_apply_visuals_inserts_broll_and_graphic. Also installed pillow+httpx into .venv-local (were missing; unblocked 13 tests).
Task 7: complete (commits 71b7cad..a23f091, review found 3 Important, all fixed + verified by controller across 5 source shapes). y-bias //4 REVERTED to //2 (plan's own snippet violated its own backwards-compat constraint; upward-bias framing dropped as unrequested embellishment). Probe-failure now falls back to legacy ffmpeg-native force_original_aspect_ratio string instead of guessing 1920x1080. Crop-bounds test given an independent regex oracle (scale=3414:1920 pinned). Root suite now 86 passed / 6 pre-existing failures.
Task 8: complete (commits a23f091..d80aab0, review clean, no fix needed). Copy verified faithful line-by-line by reviewer. Brief's test fixture had dict-vs-tuple bug for RANGES; fixed in test only, implementation untouched. PLAN ALSO PATCHED (commit after d80aab0) so Task 11 passes tuples. Minor: no test passes fonts_dir (Task 9 will).
Task 9: complete (commits d80aab0..0482ccd, review found 1 Important, fixed). Bundling the font WITHOUT wiring fontsdir had INVERTED the goal: helpers/render.py requested 'DejaVu Sans' (absent on stock Windows) with no fontsdir, guaranteeing the silent substitution the task existed to prevent. Now wired + escaped, font_available() gates it, falls back to Liberation Sans when bundle missing. PROVEN via real libass log: fontselect: (DejaVu Sans, 700, 0) -> DejaVuSans-Bold. TTF moved to assets/fonts/ttf/ so libass stops scanning README.md. Root suite 102 passed / 6 pre-existing.
Task 10: complete (commits 0482ccd..bf2440e, review clean, zero findings).
Task 11: complete (commits bf2440e..afeb2c3, review found 2 Important, both fixed). (1) build_ass return count was discarded -> a 0-byte .ass could reach ffmpeg; old renderer gated on it (render_engine.py:360-367). Now gated identically. (2) Cancel now checked per extract_segment spawn, not once per batch. Added tests pinning the (start,end) TUPLE contract and the zero-caption path. Removed unused project_dir param. 54/54 backend.
Task 12 ATTEMPT 1: BLOCKED correctly by subagent — parity gate failed on KeyError, not divergence. My test asserted old_meta["duration"] but render_export never returned that key. Geometry matched exactly (1080x1920 both); ffprobed durations 3.8 vs 3.749 (within 0.25 tolerance). Plan patched to probe both output FILES. ALSO: .venv-local had opencv-python-headless drifted to 5.0.0.93 vs pinned 4.11.0.86, which crashed the old renderer face-detection path; subagent reinstalled the pin.
Task 12: complete (commits d14a2b4..b6d8585). Parity PASSED on 2nd attempt (old 1080x1920/3.8s vs new 1080x1920/3.749s). render_engine.py DELETED; probe/make_thumbnail moved to backend/probe.py. THREE regressions found by controller e2e that the parity test could NOT catch (synthetic fixture, geometry-only assertions):
  12b df6bfb5 - face-centering was DEAD (nothing set subject_center_x; old renderer called reframe.subject_center internally). Now computed in export handler for 9:16 only + persisted. Verified live: subject_center_x=0.552.
  12b - export work dir leaked (rmtree pdir/work vs new pdir/edit/work).
  12c 928eaaf - worker crashed on Windows cp1252 printing helpers arrows; worker now calls configure_stdio().
  12d b6d8585 - libass force_style CLOBBERED karaoke ASS styling (FontSize 105->18, MarginV 288->90). force_style now skipped for .ass, kept for .srt. Measured 72px vs 12px glyphs.
E2E VERIFIED on real footage: 1080x1920, face-centered, full-size karaoke captions, Cloudinary ok.
Task 13: complete (commit b0a0ecf). scripts/dev.ps1 starts API+worker+frontend, skips already-running, health-checks API. Verified BOTH paths (fresh start and skip). README gained a ClipCut section incl. the requirements.txt trap and the opencv 4.11.0.86 pin.
NOTE (investigated, NOT a bug): "python worker.py" shows TWO python processes. The venv Scripts/python.exe is a launcher stub that re-execs the base interpreter; the CHILD runs the loop (log worker id matches child PID). Only one worker loop exists.
FINAL SUITES: backend 59 passed / 1 skipped / 0 failed. root 104 passed / 6 failed (exactly the 6 pre-existing at merge base 59404c3).
ALL 13 TASKS COMPLETE. Outstanding: final whole-branch review not yet run.
FINAL WHOLE-BRANCH REVIEW (opus, 59404c3..b0a0ecf): NOT ready to merge — 1 Critical, 7 Important.
CRITICAL: animated zooms + keyword punch-ins are SILENTLY DEAD. assemble.from_project hardcodes zoom=1.0 and never reads cut_state["moves"] (produced by zooms.plan). Old render_engine consumed moves[i] and built animated zoompan+snaps; helpers extract_segment only does STATIC zoom. Four UI controls (cinematic, zoom_intensity, punch_ins, punch_sensitivity) are now placebos. ROOT CAUSE OF MISS: my parity test ran with cinematic:False, punch_ins:False — it compared the renderers on the one config where the zoom path is inert.
IMPORTANT: (2) export.meta lost moves/punches/punch_count/caption_events -> ReelStudio "Camera moves"/"Punch-ins" stats permanently 0, two panels never render. (3) fps silently 30 -> 24 (helpers hardcodes -r 24); spec REQUIRED frame-count in parity assertion, my plan dropped it. (4) mid-render cancel hits generic except -> project error with empty message while job says cancelled. (5) reconcile_stale only at boot + project doc not reconciled on non-handler failures -> can still strand in processing. (6) per-segment progress lost (bar sits at 10%). (7) captions_ass write_text has no encoding= -> cp1252 -> mojibake/crash on non-ASCII transcripts. (8) backend<->helpers seam never tested; parity fixture is GITIGNORED so it always skips.
Reviewer triage of 10 carried Minors: fix #5 (+#4 free), rest accept.
AWAITING USER DECISION on Critical #1: restore animated zooms through EDL v2, or accept the loss and strip the dead UI controls.
FIX WAVE A (498508b,fa4a370,44c7256,c165065): restored animated zooms/punch-ins through EDL v2; fps 30 restored; parity fixture COMMITTED so the e2e render test RUNS (was always skipping); frame-count assertion added. Pixel evidence: zoom ramp ratio 1.0986 observed vs 1.0980 predicted.
FIX WAVE B (2cf99f9,a63b33d,a6c5610,d840f51,b851b58,1506e2a,c7c9e50): Cancelled caught before Exception; project reconciliation centralized so no path strands a project in processing; reconcile_stale now periodic not boot-only; per-segment progress + heartbeat; frontend STAGE_LABEL entries; ASS written UTF-8; claim only registered kinds (unknown kinds now left QUEUED, test renamed to assert that); Cancelled moved to backend/errors.py; -map 0:a tolerates silent sources; probe via hidden_proc; dead _even() deleted.
E2E VERIFIED post-fix: 1080x1920 @ 30/1 fps, 449 frames, meta has moves=7 punches=3 punch_count=3 caption_events=35 karaoke=True center_x=0.552, audio aac present, captions full-size. Progress granular (16/36/42 during cutting).
SUITES: backend 71 passed / 0 failed. root 118 passed / 6 pre-existing failed.

---

FIX-WAVE RE-REVIEW (opus, b0a0ecf..2cf99f9, 11 commits): CLEAN — 0 Critical, 0 Important. Verdict: sound enough to build on. Verified the zoompan port is line-for-line faithful to the deleted render_engine.py; parity fixture git-tracked and test runs with cinematic+punch_ins ON; reconciliation structurally complete across all terminal paths. 3 Minors, all non-blocking: build_master_srt UTF-8 (pre-existing, OUT of ClipCut caption path — only video-use CLI); cancelled-transcribe sets top-level status=cancelled (2b frontend glance); +1 ffprobe per export (negligible). GATE 1 CLEAR.

PLAN 2a written+committed (4b3ca32, reconciler add-on after). 10 TDD tasks. Folded re-review carry-forward (plan reconciler) into Task 7. Proceeding to subagent-driven execution under standing autonomy grant.

# Plan 2a: 2026-08-17-clipcut-plan2a-auto-engine (branch feat/clipcut-foundation)
Base for this plan: d9ac6c6. Backend baseline before Task 1: 71 passed/0 failed. Root: 118 passed/6 pre-existing failed.
2a-Task 1: complete (commits d9ac6c6..f269d32, review clean, no fix needed). model.validate accepts version 1|2 and center_x scalar|keyframe-list; v3 still rejected; modified pre-existing rejects_wrong_version to use v4.
2a-Task 2: complete (commits f269d32..507159c, review clean, no fix). providers/base.py: PlanContext(edit_dir,words,text,ranges,total_s), DecisionProvider protocol (name, plan(ctx)->dict|None), validate_picks. 8 tests.
2a-Task 3: complete (commits 507159c..f4880b2, review clean). HeuristicProvider: b-roll from 5+ char non-stop words (cap 3), graphics from HOOK_WORDS (cap 3), always valid picks, never None. Minor(final-triage): dead 'elapsed' var in _range_of; redundant float() calls heuristic.py:94-95.
2a-Task 4: complete (commits f4880b2..2e42a5c, review clean). overlays_from_picks: visuals->broll(mixkit)/still(pexels) overlays via output_time_at, graphics->pil, file=None, locked prepended verbatim. Minor(final-triage): unused Path import; ranges type hint 'list' vs 'list[dict]'; duration clamp is plan-mandated+intentional (mirrors apply_visuals), NOT a defect.
2a-Task 5: complete (commits 2e42a5c..1d85eb0, review found 2 Important plan-mandated, both fixed+re-reviewed clean). resolve_overlays: broll image results now converted via photo_to_clip (was: JPG handed to compositor as video); per-overlay fault isolation so a raising helper degrades that overlay to graphic without aborting batch. Minor(final-triage): redundant _broll_dir call; no logging when an overlay silently degrades to graphic/None.
2a-Task 6: complete (commits 1d85eb0..a12ecdb, review clean, no fix). render_plan._composite resolves enabled overlays via ov_mod.resolve_overlays before build_final_composite; empty list when none. 8 tests.
2a-Task 7: complete (commits a12ecdb..3d1bfe8, review clean, no fix). plan job: fetch->cut_state->assemble->materialize->provider chain (heuristic-only)->overlays_from_picks->store plan/plan_provider/plan_status=ready. regenerate preserves locked. _reconcile_plan_project registered so a failed plan job sets plan_status=error not stranded at planning. Fixed pre-existing test_jobs unknown-kind test (was using 'plan' as example unregistered kind). Minor(final-triage): dead post-loop heuristic fallback (plan-mandated, in-loop heuristic never returns None); regenerate test could assert count==1.
2a-Task 8: complete (commits 3d1bfe8..4d63b2b, review found 2 Important plan-mandated, both fixed+re-reviewed clean). ClaudeCliProvider: write brief->run claude one-shot(180s,killed on expiry)->read+validate picks.json, None on any failure(never raises), prepended to chain. FIXES: (1) now checks exit code before reading picks.json (was: stale picks.json could return after claude failed); (2) handler tests stubbed so no real claude spawn (were 21s/44s, now 0.06s/0.09s); added opt-in test_real_claude_end_to_end gated on CLIPCUT_TEST_CLAUDE=1. Minor(final-triage): autouse-stub opt-out uses literal test name (rename coupling).
2a-Task 9: complete (commits 4d63b2b..e732d8e, review found 1 Important route-test gap, fixed test-only). transcribe success auto-enqueues plan job; POST/GET /api/projects/{pid}/plan routes; export attaches stored plan's ENABLED overlays onto EDL (additive, export sub-doc unchanged). Added test_plan_routes.py (6 route tests via TestClient+monkeypatched clipcut_test) + disabled-overlay exclusion test. Full suite 122 passed/1 skipped.
2a-Task 10: complete (commits e732d8e..b66119a, review clean, no fix). Headless e2e: heuristic plan -> overlays -> real ffmpeg render of committed fixture, offline (broll/still fetch stubbed -> PIL graphic fallback). Asserts >=1 overlay, 1080x1920, duration>0. Rendered 26.5s (not skipped). Full backend suite 123 passed/1 skipped.
PLAN 2a REAL-APP E2E VERIFIED: upload -> transcribe -> AUTO-plan job fired -> plan_provider=CLAUDE (real claude CLI ran, ~40s, produced valid picks.json). Claude chose 7 contextual overlays from the transcript: still "subtitle captions screen", broll "film reel footage", broll "laptop video editing", still "github code repository", graphics CAPTIONS/B-ROLL/AI. Export fetched 2 real Mixkit clips, composited b-roll over the talking head at the right moments with karaoke captions on top, 1080x1920. Frames at 3.9s (film-reel broll) and 6.5s (laptop-typing broll, matched to speaker saying "b-rolls and graphics") confirm compositing. THE FULL VISION WORKS. Test project deleted after.
ALL 10 PLAN-2a TASKS COMPLETE. Next: final whole-branch review of plan-2a range.
FINAL WHOLE-BRANCH REVIEW of Plan 2a (opus, d9ac6c6..b66119a, 13 commits): 0 Critical, 2 Important, verdict "with fixes". BOTH Important FIXED in da72098:
  1. heuristic graphics used SOURCE time not OUTPUT time (keyword pops landed late by trimmed footage) -> added _source_to_output mapping.
  2. resolve_overlays file=None (fallback-of-fallback) crashed whole export via Path(None) in build_final_composite -> _composite now drops file-less overlays.
  Also: degradation logging + overlay["degraded"] flag (spec wants degraded surfaced); removed dead code (elapsed accumulator, unreachable post-loop heuristic fallback).
Final-review Minors triaged: mostly accept; the no-logging one was fixed. All 6 deferred Minors resolved or accepted.
PLAN-LEVEL NOTE for Plan 2b: overlay start_in_output is frozen at plan time but export re-assembles base EDL from CURRENT settings — a user who regenerates the plan then changes cut settings gets overlays timed against the old timeline. Plan 2b should re-plan on settings change or clamp/flag overlays past the reassembled total.
BACKEND SUITE: 125 passed / 1 skipped (the skip is CLIPCUT_TEST_CLAUDE-gated real-claude test). Root helpers suite unaffected (fixes are backend-only).
=== PLAN 2a COMPLETE. Branch feat/clipcut-foundation, HEAD da72098. Not merged to main. ===
